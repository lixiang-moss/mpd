# planner=dummy: a virtual planner for smoke tests/unit tests.
# It writes trial_metrics.jsonl to validate adapter/aggregation/ranking/plotting logic end-to-end.

name: dummy
python: python3
entrypoint: dmeval/scripts/dummy_planner.py
workdir: .
extra_args: []
