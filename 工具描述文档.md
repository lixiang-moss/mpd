---
title: DMEval vNext 需求/架构说明（给另一个 Codex 用）
status: 作为实现规格（可直接照做）
scope: 串行；以 MPD 为首个适配器；Hydra 只在 DMEval（L1）
---

# DMEval vNext：最终架构与实现规格（L1 方案）



> 核心结论（你已确认）：  
> 1) **Hydra 只用于 DMEval 的配置组合（L1），不改 MPD**；  
> 2) 结果提取优先读 `trial_metrics.jsonl`（若 MPD 做轻量输出），否则回退读 `.pt`；  
> 3) Stage I 调参使用“**阈值 + 排序/评分**”范式；  
> 4) 贝叶斯/Optuna 调参的更新时机是“**一个 candidate 完整评估结束（N trials + 聚合）后再更新**”，不是按单条 trial 实时更新；  
> 5) 接受对 MPD 做“轻量改动”以换取便利性（增加 `trial_metrics.jsonl` / 可选 `run_summary.yaml`）。

## 0. 总纲领（不可违背原则，避免“过拟合细节”）

> 这部分优先级最高：当某些细节描述与实际项目冲突时，**以这些原则为准**做取舍。

- **L1 解耦**：DMEval 只负责“编排运行 + 抽取/聚合/比较”，被测系统（如 MPD）按自身方式推理并产出指标；DMEval 不侵入其实现细节。
- **配置驱动**：实验组合/覆盖应通过配置实现（Hydra compose + overrides）；避免靠“改脚本/改代码”来做实验矩阵除非轻量的改动可以带来比较好的效果或者更方便简洁
- **不写死路径**：被测系统入口命令、base_cfg 路径、输出根目录都应可配置；文档里的路径仅作为示例，不应当作硬约束。
- **只抽取、不重算**：指标数值由被测系统产出；DMEval 不重算轨迹指标，只做字段抽取、聚合统计与可视化。

- **两阶段范式**：Stage I（调参）产出每个 sampler 的 `best_configs`；Stage II（对比）读取 best_configs 做跨场景公平对比。
- **只串行**：默认实现只跑串行子进程；不引入并行调度/队列/分布式（未来要加并行也不能破坏输出契约）。
- **安全与可追溯**：输出只写到 `pipeline_root` 管理的目录；不覆盖已有结果（除非明确配置）；每次运行必须能追溯到“用了哪些配置/参数/seed/N”。
- **不依赖外部 sweep 脚本**：不把被测系统自带的 sweep/launch 脚本当作数据来源或调参引擎；DMEval 只面向规范化产物目录与接口契约。

---
## 1. 目标与边界（必须严格遵守）

### 1.1 目标
DMEval 是一个**评测管线工具**，用于比较扩散采样器在机械臂运动规划中的速度–质量权衡。它要提供：
- 两阶段流程：Stage I 调参 → Stage II 跨场景公平对比
- 统一的数据产物：CSV（trial/run/rank）+ 基础图表 + best_configs 固化
- 配置驱动：通过 Hydra 组合/覆盖实验配置（而不是手改大量脚本）

### 1.2 明确边界（不可越界）
- DMEval **不计算指标数值**（指标由被测系统在推理阶段产出；DMEval 只做抽取/聚合/比较）。
- DMEval **不依赖** MPD 的 sweep 脚本（`run_*_sweep.py`、`launch_inference-experiments.py`）作为“数据来源”；它只读规范化产物目录。
- **只做串行**：不引入复杂并行/任务队列/分布式。

---
## 2. 关键术语（统一口径，避免概念混乱）

> 必须在文档、日志、CSV、目录名里坚持这些词。

- Scenario：一个评测场景（在 MPD 里对应一个 base inference YAML）
- Sampler：一个采样器（ddpm/ddim/dpm_solver/dpm_solver_pp/unipc…）
- Candidate：一个候选超参配置（例如 steps=60, order=2, pw=1.5）
- Run：一次运行 = Candidate × Seed（一次调用 inference）
- Trial：一次 run 内，对一个 start-goal 的一次规划尝试（MPD 里通常对应一个 `results_single_plan-XXX.*` 或 jsonl 的一行）
- Seed：随机种子（固定抽样与随机性；用于复现与公平对比）
- N：`n_start_goal_states`，一次 run 内 trial 的数量

> 重要：  
> “只用一个 seed（例如固定 seed=2）”是允许的（开发阶段），但 seed 仍要显式记录；最终论文结果推荐多 seed（提升稳健性与可复现性）。

---
## 3. 总体架构（L1：Hydra 只在 DMEval）

### 3.1 用户视角流程图（你要做什么）

```
写/改 DMEval Hydra 配置（defaults + overrides）
        │
        v
Stage I: tune（单场景）
  - 生成候选 candidates（grid / random / optuna）
  - **一键调优多个采样器**：同一份配置里列出多个 sampler，DMEval 在一次 `tune` 中按顺序逐个跑完
  - 串行跑 MPD：candidate × seeds（每次 run 内 N trials）
  - Collect：trial -> run 聚合
  - Objective：阈值过滤 + 排序/评分
  - 输出 best_configs/<sampler>/{best_patch.yaml,best.yaml,topk.yaml}
        │
        v
Stage II: compare（多场景）
  - 读取 Stage I 的 best_patch.yaml
  - 对每个 scenario base_cfg 打 patch，生成最终 cfg
  - 串行跑 MPD：scenario × sampler × seeds（每次 run 内 N trials）
  - Collect + Rank + Plot
```

### 3.2 组件架构图（实现者视角）

```
┌─────────────────────┐
│  DMEval CLI          │  tune / compare / collect / plot / explain(plan可选)
└─────────┬───────────┘
          │
          v
┌─────────────────────┐
│ Hydra Loader (compose)│  只负责：defaults 组合 + overrides 覆盖
└─────────┬───────────┘
          │
          v
┌─────────────────────────────────────────────────────────┐
│ Pipeline Orchestrator                                   │
│  - Stage I: SearchStrategy -> CandidateGenerator         │
│  - Runner (serial subprocess)                            │
│  - Collector (trial->run)                                │
│  - Objective (constraints + score / lexicographic)       │
│  - BestConfigsWriter                                     │
│  - Stage II: BestLoader + ScenarioExpander + Runner...   │
└─────────┬───────────────────────────────────────────────┘
          │
          v
┌─────────────────────┐
│ Adapter Layer        │  MPDAdapter: 读 jsonl 或 pt + args_inference.yaml
└─────────┬───────────┘
          │
          v
┌─────────────────────┐
│ MPD (被测)           │  计算指标并写出产物
└─────────────────────┘
```

---
## 4. Hydra 在本项目中的“正确用法”（你要求的 C1）

### 4.1 Hydra 做什么
只做：**配置组合/覆盖**  
- `defaults`：组合 `scenario / planner / objective / search`
- `--override`：运行时临时覆盖（device、N、seeds、某个搜索空间等）

### 4.2 Hydra 不做什么
不让 Hydra 接管：
- cwd 切换（避免相对路径坑）
- 自动 `outputs/` 目录体系
- 并行 launcher（本项目只串行）

实现建议：
- DMEval 使用 Hydra **compose API** 加载配置（不是 `@hydra.main`）。
- 因此用户不会看到 Hydra 的 `.hydra/outputs` 痕迹，但 `defaults/override` 仍然生效。

---
## 5. 与 MPD 的交互契约（输入/输出）

### 5.1 DMEval 调用 MPD（串行 subprocess）
每个 run（candidate × seed）执行一次：

> 说明：下方命令以 MPD 为例。实现新工具时**不要把入口脚本路径写死**，应该由配置提供（例如 `<planner_entry>`）。  
> 例如：MPD 仓库里常见入口脚本是 `scripts/inference/inference.py`（相对于 MPD 仓库根目录），但你的目录名/入口形式可以不同。

```
python3 <planner_entry> \
  --cfg_inference_path <cfg_yaml> \
  --results_dir <pipeline_root>/<stage>/<run_tag> \
  --seed <seed> \
  --selection_start_goal validation \
  --n_start_goal_states <N> \
  --save_results_single_plan_low_mem true \
  --device cuda:0
```

注意：MPD 侧 experiment_launcher 会把结果落到 `<results_dir>/<seed>/...`（seed 子目录由 MPD 自动创建）。

### 5.2 MPD 必需产物（0 改动也能跑）
每个 seed 目录至少包含：
- `args_inference.yaml`
- `results_single_plan-000.pt` …（N 个 trial 对应 N 个 `.pt`）

> 注：以上是 MPD 当前默认文件名/模式。新工具实现中不要把这些字符串散落在各处；应集中在 Adapter 内定义（必要时允许通过配置覆盖）。

### 5.3 MPD 推荐的轻量产物（你已接受：H）
为提升通用性/可移植性/贝叶斯调参效率，允许对 MPD 做轻量改动：

在每个 seed 目录额外写：
- `trial_metrics.jsonl`（每个 trial 一行 JSON；只包含指标与时间等轻量字段）
- （可选）`run_summary.yaml`（该 seed 目录内 N 个 trial 的聚合 mean/std）

**run_summary.yaml 的用途**（回答你的第 5 问）：  
它让 DMEval/Optuna 在“一个 candidate 的一次 run 完成”后，能**直接读到聚合统计**，避免扫描大量 trial 文件；也便于后续做贝叶斯/早停/报表。

---
## 6. Stage I：调参（F：阈值 + 排序/评分；支持贝叶斯的更新方式）

### 6.1 Candidate 的评估单位（贝叶斯更新的正确粒度）
无论 grid/random/optuna，评估单位都是：

> evaluate(candidate) = 在 seeds 上运行 MPD（每次 run 内 N trials） → 聚合成 run 统计 → 得到一个 score

因此贝叶斯更新是：
- 提出 candidate
- **等该 candidate 的完整统计结果出现**（N trials 聚合完成）
- 再 `tell(score)` 更新模型，提出下一个 candidate

这正是你确认的“不是实时按 trial 更新”的逻辑。

### 6.2 Objective（目标函数）结构化要求
Objective 必须分两层（可配置）：

1) **Constraints（阈值过滤）**：不满足直接淘汰  
示例（只是占位，最终你自己定阈值）：
- `success_mean >= 0.8`
- `fraction_valid_mean >= 0.6`
- `collision_intensity_mean <= 0.05`

2) **Score 或 Lexicographic 排序**：在可行集合内选 best  

- **A：Lexicographic（多指标字典序排序）**（先不引入权重，便于跑通闭环）  
  例：把每个 run 的聚合统计映射成一个 key tuple，然后做排序：  
  `key = (-success_mean, collision_intensity_mean, time_mean, path_len_mean, ...)`  
  本质上就是“先过 constraints，再按 tuple 排序取 top_k”。

- **B：标量 score（Optuna/贝叶斯更友好）**  
  例如：`score = success_mean - α*time_mean - β*collision_mean - γ*path_len_mean ...`

> 建议：先用 A（字典序排序）跑通闭环（不纠结权重）；当要上 Optuna 时再切到 B（标量 score），或用“阶梯惩罚/大权重”把 A 近似映射成标量。

### 6.3 搜索策略（SearchStrategy）要求
必须支持：
- `grid`（离散列表笛卡尔积）
- `random`（范围/分布采样；用于连续参数）
- `optuna`（TPE/贝叶斯；串行；P1 可选实现）

并推荐提供 MPD/dpm_solver 的 **coarse→refine** 策略：
- coarse：扫离散结构参数（steps/order/ct/solver++)
- refine：锁定 coarse top_k，扫连续参数（pw、guide_lr、max_perturb_x、t_start…），用 random 或 optuna

Stage I 输出：
- `best_configs/<sampler>/best_patch.yaml`（必须）
- `best_configs/<sampler>/best.yaml`（可选：完整 cfg）
- `best_configs/<sampler>/topk.yaml`（必须：便于复查）
- `tune_manifest.yaml`（必须：记录 search_space、seeds、N、objective 版本、时间等）

### 6.4 “一键调优多个采样器”（你问的“只发一条命令行可以吗？”）
这里的“多个采样器”指的是：**你在配置里明确列出来、你想调的那几个 sampler**（不是“工具支持的全部 sampler”）。

要求：一次 `dmeval tune` 调用必须能把 `tune.samplers` 列表里的 sampler **按顺序**调完；不在 `tune.samplers` 里的 sampler **不会被跑**。

- 你只需要运行一次命令：`dmeval tune --config <xxx.yaml> [--override ...]`
- DMEval 内部串行循环：`for sampler in tune.samplers: ...`
- tune 结束后，你应该能在同一个 `pipeline_root` 下同时看到多个采样器的最优配置：
  - `best_configs/ddpm/best_patch.yaml`
  - `best_configs/dpm_solver/best_patch.yaml`
  - `best_configs/dpm_solver_pp/best_patch.yaml`
  - ...

---
## 7. Stage II：跨场景公平对比（compare）

输入：
- Stage I 产出的 `best_configs_root`
- `compare.scenarios`（多个 base_cfg）
- `compare.seeds`、`common_inference_args.n_start_goal_states=N`

流程：
- 对每个 scenario 的 base_cfg 应用 best_patch
- 串行跑 MPD：scenario × sampler × seed
- Collect 聚合成 `run_metrics.csv`
- Rank + Plot（输出排名表与图）

输出：
- `run_metrics.csv` / `rank_*.csv` / `plots/`
- `compare_manifest.yaml`

---
## 8. Collect/Plot（先聚合再分析）

Collect 必须产出两层 CSV：
- `trial_metrics.csv`：每个 trial 一行（来自 `.pt` 或 `trial_metrics.jsonl`）
- `run_metrics.csv`：按 `(scenario, run_tag, seed)` 聚合 mean/std（用于公平比较）

### 8.1 指标字段口径（按 MPD 现有输出固化；你确认“就按这个来”）
以 MPD 输出的 `metrics.*` 字段为准，DMEval 只做“抽取/聚合”，不重算。

**Trial 级（每个 trial 一行）必须至少包含：**
- 元信息：`scenario`, `run_tag`, `seed`, `trial_id`
- 采样器/方法：`planner_alg`, `diffusion_sampling_method`（以及可选：`sampler_name`/`candidate_id`）
- 时间：`t_inference_total`, `t_generator`, `t_guide`
- 可行性/安全（来自 `metrics.trajs_all.*`）：
  - `success`
  - `fraction_valid`
  - `collision_intensity`
- 精度（来自 `metrics.trajs_best.*` / `metrics.trajs_all.*` / `metrics.trajs_valid.*`）：
  - `ee_pose_goal_error_position_norm_best`
  - `ee_pose_goal_error_orientation_norm_best`
  - `ee_pose_goal_error_position_norm_mean_all`, `ee_pose_goal_error_position_norm_std_all`
  - `ee_pose_goal_error_orientation_norm_mean_all`, `ee_pose_goal_error_orientation_norm_std_all`
  - `ee_pose_goal_error_position_norm_mean_valid`, `ee_pose_goal_error_position_norm_std_valid`
  - `ee_pose_goal_error_orientation_norm_mean_valid`, `ee_pose_goal_error_orientation_norm_std_valid`
- 轨迹质量（来自 `metrics.trajs_best.*` / `metrics.trajs_valid.*`）：
  - `path_length_best`
  - `path_length_valid_mean`, `path_length_valid_std`
  - `smoothness_best`
  - `smoothness_valid_mean`, `smoothness_valid_std`
- 多样性（来自 `metrics.trajs_valid.diversity`）：`diversity`（可能为 null/NaN）

**Run 级（按 candidate×seed 聚合）至少包含：**
- `scenario`, `run_tag`, `seed`
- 上述数值字段的 `*_mean` 与 `*_std`（最低要求：success/fraction_valid/collision/time/path_length/smoothness）

> 实现注意：若 MPD 做了轻量输出（`trial_metrics.jsonl` / `run_summary.yaml`），字段名应与上面保持一致；否则从 `.pt` 的 `result.metrics` 中按相同字段抽取。

Rank 默认至少输出：
- `rank_success.csv`
- `rank_fraction_valid.csv`
- `rank_path_length.csv`
- `rank_speed.csv`

Plot 至少输出：
- success / fraction_valid / time 的柱状图
- time vs success 的散点图

---
## 9. Adapter 设计（保证通用性/可扩展）

必须把“planner 结果解析”隔离成 Adapter：
- `discover_trials(results_root)`：发现 trial 产物
- `load_trial_object(artifact)`：加载（jsonl/pt/npz…）
- `extract_row(...)`：转成一行 dict（统一字段 + 可选动态字段）

MPDAdapter 的要求：
- 首选读取 `trial_metrics.jsonl`（若存在）
- 否则读取 `.pt`（torch.load）
- `args_inference.yaml` 用于补充配置元信息（planner_alg、diffusion_sampling_method、env_id_replace…）

---
## 10. 不做/延后做的事情（避免范围膨胀）
- 不做复杂并行/队列（只串行）
- 不依赖 MPD sweep 脚本作为数据来源
- 不在 DMEval 里重算轨迹指标
- 报告生成（HTML/PDF）可后置

---
## 11. 最小验收标准（交付给导师前必须满足）

1) 用同一份 DMEval 配置，可以跑完整闭环：Stage I → best_configs → Stage II → rank/plots  
2) 任意一次运行都能复现（manifest 记录完整；目录结构固定）  
3) 结果聚合口径清晰：trial/run/seed/candidate 定义在文档与输出中一致  
4) 允许单 seed（开发），但 seed 必须显式记录；最终可扩展到多 seed


## 12.开发时注意！

- 我无法在docker环境中使用你，所以我让你在宿主机环境写代码。但是运行时我会打开容器来运行，因此你要自建/补充 .md 文件中说明需要添加哪些依赖

- 你要写一个.md文件详细的交待如何使用这个工具。可以以mpd为例子

- 最好再以 MPD 为被测试对象给我一个最小端到端测试样例（从安装依赖到跑 tune/compare 再到产出 CSV/plots），这样新工具完成后我可以直接运行验证闭环。
